server:
  port: 8087

profiles:
spring:
  profiles:
    active: druid
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
    in-memory: true
    pool:
      enabled: false